# Compiling
Run install the dependencies ```./deps.sh``` and compile with ```./compile.sh```
# Acknowledgements 
Thanks to fastfetch for the ascii art (see https://github.com/fastfetch-cli/fastfetch)
